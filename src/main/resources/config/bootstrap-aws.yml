# ===================================================================
# Spring Cloud Config bootstrap configuration for the "aws" profile
# ===================================================================

spring:
  application:
    name: cloudappwatch
  cloud:
    config:
      fail-fast: true

cloud:
  aws:
    stack:
      name: cloudappwatch
      auto: false
